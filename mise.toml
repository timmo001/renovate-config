[tasks.check]
description = "Validate the shared Renovate preset"
run = '''
docker run --rm --volume "$PWD:/work:ro" --workdir /work \
  ghcr.io/renovatebot/renovate:44.103.0 \
  renovate-config-validator --strict --no-global default.json
'''
